--- /dev/null
+++ ../src/Application/FileDecorator/FileDiffFileDecorator.php
@@ -25,8 +25,9 @@
             if (!$file->hasChanged()) {
                 continue;
             }
-            $fileDiff = $this->fileDiffFactory->createFileDiff($file, $file->getOriginalFileContent(), $file->getFileContent());
-            $file->setFileDiff($fileDiff);
+
+            $fileDiffs = $this->fileDiffFactory->createFileDiff($file, $file->getOriginalFileContent(), $file->getFileContent());
+            $file->setFileDiff($fileDiffs);
         }
     }
 }
